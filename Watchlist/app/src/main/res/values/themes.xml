<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Watchlist" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryLightColor</item>
        <item name="colorOnPrimary">@color/primaryTextColor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorSecondaryVariant">@color/secondaryLightColor</item>
        <item name="colorOnSecondary">@color/secondaryTextColor</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
<!--        TextAppearance.AppCompat.Display2-->
        <item name="android:buttonStyle">@font/cabin</item>
    </style>

    <style name="Theme.Watchlist.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="BodyText" parent="Theme.Watchlist">
        <item name = "android:fontFamily">@font/cabin</item>

    </style>
    <style name="parentText" parent="Theme.Watchlist">
        <item name = "android:fontFamily">@font/belgrano</item>
    </style>

    <style name="Theme.Watchlist.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Watchlist.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>